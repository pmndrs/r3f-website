const path = require(`path`);

// exports.createPages = ({ actions }) => {
//   const { createPage } = actions;
//   const home = path.resolve(`src/pages/index.js`);

//   createPage({
//     // Path for this page — required
//     path: `/`,
//     component: home,
//   });
// };
